# Ansible-Wireguard
This Ansible WireGuard playbook automatically installs and configures a complete WireGuard VPN. It generates all server and client keys, builds 10  wg0.conf files, sets up routing and firewall/NAT rules, and enables the interface to start on boot. Providing a fully automated, repeatable, and secure VPN deployment with a single command.
